// Return float with 3 decimal places
const threeDecimals = (num) => {
    return Number.parseFloat(num).toFixed(3);
}

// Check if the date is within this week or if its in the future
const thisWeek = (today, departure) => {

}

export { threeDecimals, thisWeek }

